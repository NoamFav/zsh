# ── AI and multimedia ───────────────────────────────────────────────────────

alias llama3='llama-cli -hf unsloth/Llama-3.3-70B-Instruct-GGUF'

# AppleScript, since Music.app has no real CLI
alias mm="osascript -e 'tell application \"Music\" to playpause'"
alias mn="osascript -e 'tell application \"Music\" to next track'"
alias mp="osascript -e 'tell application \"Music\" to previous track'"

alias starwars='ssh starwarstel.net' # yes, a Star Wars MUD
alias idle='pipes.sh -p 10 -t 9 -r 100000 -R'
